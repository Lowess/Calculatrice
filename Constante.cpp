#include "Constante.h"

Calculatrice::Constante& addition(const Constante& c) const {
    //Faire le calcul
    //Retourner une Constante pour l'insérer dans la pile
}
Calculatrice::Constante& soustraction(const Constante& c) const {
    //Faire le calcul
    //Retourner une Constante pour l'insérer dans la pile
}
Calculatrice::Constante& multiplication(const Constante& c) const {
    //Faire le calcul
    //Retourner une Constante pour l'insérer dans la pile
}
Calculatrice::Constante& division(const Constante& c) const {
    //Faire le calcul
    //Retourner une Constante pour l'insérer dans la pile
}
